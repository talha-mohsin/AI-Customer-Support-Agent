# Agent Flow

> Status: placeholder. The LangGraph agent, RAG pipeline, and tool-calling are Day 2 work (see [Agent.md](../Agent.md)). This document will be filled in once that code exists — describing what was actually implemented and verified, not planned behavior.
